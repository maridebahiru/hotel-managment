{
  "name": "hotel-digital-menu-root",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client:dev": "npm run dev --workspace=client",
    "server:dev": "npm run dev --workspace=server",
    "dev": "npm run dev --workspaces --if-present"
  },
  "devDependencies": {
    "prisma": "5.22.0"
  }
}
